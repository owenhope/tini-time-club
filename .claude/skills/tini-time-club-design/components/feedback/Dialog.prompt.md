Destructive confirmation on web. Keep the copy plain — no jokes in a delete dialog.

```jsx
<Dialog open={open} title="Delete this review?" confirm="Delete" onClose={close}>It leaves your journal for good.</Dialog>
```
