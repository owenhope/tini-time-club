Icon-only pill button for toolbars, cards and app bars.

```jsx
<IconButton icon="heart" label="Save" active />
<IconButton icon="chevron-left" label="Back" tone="glass" />
```

`glass` uses the translucent blur treatment for controls sitting over photography.
